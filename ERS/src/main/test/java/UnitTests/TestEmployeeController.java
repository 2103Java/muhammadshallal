package UnitTests;

public class TestEmployeeController {

	//Make sure that a classic employee with a non-assigned manager email can't register as a manager
	//	username = generateAlphaNumericString()+"@trial.com";
	//	password = generateAlphaNumericString();
	//	employee.setEmail(username);
	//	employee.setPassword(password);
	//	employee.setIsManager(true);
	//	
	//	assertFalse(EmployeeServiceImpl.getInstance().registerEmployee(employee));

}
